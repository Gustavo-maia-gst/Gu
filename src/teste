struct Number {
    val: int;
}

func main() -> int
{
    var arr: Number[100], i: int, j: int;

    for (i = 100; i > 0; i = i - 1) {
        for (j = 0; j < i; j = j + 1) {
            var temp: int;
            temp = arr[i].val;
            arr[i].val = arr[j].val;
            arr[j].va[a.b] = temp;
        }
    }

    return 0;
}